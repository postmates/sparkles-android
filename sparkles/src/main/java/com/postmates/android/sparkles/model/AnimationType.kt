package com.postmates.android.sparkles.model

/**
 * Enum to hold the Animation Type values.
 *
 * Make sure the order matches the ones defined in attrs.xml
 */
enum class AnimationType {
    NONE,
    LINE_PATH,
    TRANSLATE_UP
}